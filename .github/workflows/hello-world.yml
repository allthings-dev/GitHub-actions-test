name: Hello World
run-name: Hello World Instance

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, closed]
  push:
    branches:
      - main
  
jobs:
  HelloWorldJob:
    runs-on: ubuntu-latest
    steps:
    - name: Echo Step
      run: echo "Hello World"
